import ResetPasswordScreen from "@/components/authentication/ResetPasswordScreen";

/**
 * Route: /(authentication)/reset-password
 * Renders the Reset Password Screen component only. Do not add business
 * logic or navigation logic here — see
 * components/authentication/ResetPasswordScreen.tsx.
 */
export default function ResetPasswordPage() {
  return <ResetPasswordScreen />;
}
