import LoginScreen from "@/components/authentication/LoginScreen";

/**
 * Route: /(authentication)/login
 * Renders the Login Screen component only. Do not add business logic
 * or navigation logic here — see components/authentication/LoginScreen.tsx.
 */
export default function LoginPage() {
  return <LoginScreen />;
}
