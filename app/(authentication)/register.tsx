import RegisterScreen from "@/components/authentication/RegisterScreen";

/**
 * Route: /(authentication)/register
 * Renders the Register Screen component only. Do not add business logic
 * or navigation logic here — see components/authentication/RegisterScreen.tsx.
 */
export default function RegisterPage() {
  return <RegisterScreen />;
}
